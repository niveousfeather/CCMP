# 能力图谱工作区占位

本目录预留给「能力图谱」前端工作区。

当前状态：规划骨架。

本轮不实现页面、不接入真实 API、不调用模型、不读取真实图谱数据。

第一版规划专业：具身智能。

开发边界：

- 只允许在 `capability-map` 相关目录内继续开发。
- 不要修改 Word、PPT、Excel、图片生成、视频生成、3D 工作区、知识图谱、普通聊天、Agent 主链路、模型路由、用户系统、历史记录和设置页。
- 若后续需要导航入口，应保持最小改动，并在交接文档中记录影响范围。
