# 能力图谱服务占位

本目录预留给「能力图谱」领域服务、类型和数据处理逻辑。

当前状态：只包含类型草案和架构说明。

后续所有搜索、摘要、图谱生成、证据归档、导出、定时刷新等逻辑，都应封装在本目录的独立 service 中。

禁止事项：

- 不要修改现有模型配置。
- 不要修改 Word/PPT/Excel/图片/视频/3D/知识图谱/普通聊天的调用链路。
- 不要把 capability-map 逻辑写入 Agent 主路由。
- 不要复用 Word/PPT 的业务生成链路。
