# Future Python backend

Reserved for a future isolated paper-ppt-agent backend.

Do not add Python dependencies to the Next.js package.json or main build flow.
The sidecar should run as a separate service and expose HTTP APIs consumed by
`/api/smart-tools/academic-ppt/*`.
